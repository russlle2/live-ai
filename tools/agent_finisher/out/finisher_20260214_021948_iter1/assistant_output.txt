{'id': 'resp_083773e6c347429d006990221c0ec081a38217f4d1b0875fc0', 'object': 'response', 'created_at': 1771053596, 'status': 'completed', 'background': False, 'billing': {'payer': 'developer'}, 'completed_at': 1771053735, 'error': None, 'frequency_penalty': 0.0, 'incomplete_details': None, 'instructions': 'You generate high-quality code patches and files. Output must be structured exactly as requested.', 'max_output_tokens': None, 'max_tool_calls': None, 'model': 'gpt-5-2025-08-07', 'output': [{'id': 'rs_083773e6c347429d006990221cdb2c81a3893bfebac846de98', 'type': 'reasoning', 'summary': []}, {'id': 'msg_083773e6c347429d0069902256909c81a39c8cce5e2a836457', 'type': 'message', 'status': 'completed', 'content': [{'type': 'output_text', 'annotations': [], 'logprobs': [], 'text': 'PATCH_START\n*** Begin Patch\n*** Add File: packages/shared/src/heuristics/patterns/stakeholderSignOff.ts\n+/**\n+ * Heuristics: Stakeholder sign-off / procurement detection\n+ *\n+ * Goal:\n+ * - Reduce moment="unknown" by catching procurement/legal/security/CFO approval language.\n+ * - Keep logic purely regex-based to avoid changing protocol types.\n+ *\n+ * Notes:\n+ * - These exports are additive and safe. They can be optionally wired into existing detectors.\n+ * - Do not log raw transcript content here.\n+ */\n+\n+export const STAKEHOLDER_SIGN_OFF_PATTERNS: RegExp[] = [\n+  // Direct approval/sign-off language\n+  /\\b(signs?\\s*[- ]?\\s*off|sign[- ]?off)\\b/i,\n+  /\\bapprov(e|al|ed|ing)\\b/i,\n+  /\\bgreen[- ]?light\\b/i,\n+\n+  // Budget / finance roles\n+  /\\bcfo\\b/i,\n+  /\\b(finance|financial)\\b/i,\n+  /\\bbudget(\\s+(owner|holder))?\\b/i,\n+\n+  // Procurement / purchasing\n+  /\\bprocure(ment)?\\b/i,\n+  /\\bpurchase\\s+order\\b/i,\n+  /\\b(?<!\\w)po(?!\\w)\\b/i, // standalone "PO"\n+\n+  // Legal and security review\n+  /\\blegal\\b/i,\n+  /\\b(msa|dpa)\\b/i,\n+  /\\bsecurity\\s+review\\b/i,\n+  /\\b(info\\s*sec|infosec|secops)\\b/i,\n+];\n+\n+/**\n+ * Returns true if the text likely references stakeholder approval,\n+ * procurement, legal, or security review checkpoints.\n+ */\n+export function detectStakeholderSignOff(text: string): boolean {\n+  if (!text) return false;\n+  return STAKEHOLDER_SIGN_OFF_PATTERNS.some((re) => re.test(text));\n+}\n+\n+/**\n+ * Lightweight classifier for stakeholder-related moment.\n+ * Returns a score (0..1) that can be blended with existing models.\n+ * - Only uses regex presence count; capped and normalized.\n+ */\n+export function scoreStakeholderSignOff(text: string): number {\n+  if (!text) return 0;\n+  const matches = STAKEHOLDER_SIGN_OFF_PATTERNS.reduce((acc, re) => {\n+    return acc + (re.test(text) ? 1 : 0);\n+  }, 0);\n+  // Cap at 4 signals to keep confidence conservative\n+  const capped = Math.min(matches, 4);\n+  return capped / 4;\n+}\n+\n*** End Patch\n*** Begin Patch\n*** Add File: packages/shared/src/heuristics/patterns/styleConstraints.ts\n+/**\n+ * Heuristics: Style constraint detection\n+ *\n+ * Goal:\n+ * - Avoid classifying style modifiers as unknown moment.\n+ * - Extract guidance like "short version", "non-technical", "answer today".\n+ *\n+ * No side effects; safe to import from existing detectors/coaches.\n+ */\n+\n+export type BrevityLevel = \'short\' | \'one_sentence\';\n+export type TechnicalLevel = \'non_technical\' | \'technical\';\n+export type TimeframeHint = \'today\' | \'this_week\';\n+\n+export interface StyleConstraints {\n+  brevity?: BrevityLevel;\n+  technicalLevel?: TechnicalLevel;\n+  timeframe?: TimeframeHint;\n+  directness?: boolean; // e.g., "straight answer"\n+}\n+\n+const RE_BREVITY_SHORT =\n+  /\\b(tl;?dr|short\\s+version|keep\\s+it\\s+short|quick\\s+(one|question|answer)|one[-\\s]?liner|one\\s+liner)\\b/i;\n+const RE_BREVITY_ONE_SENTENCE = /\\b(one\\s+sentence|in\\s+one\\s+sentence)\\b/i;\n+const RE_NON_TECHNICAL =\n+  /\\b(non[-\\s]?technical|explain\\s+like\\s+i\'?m\\s+five|keep\\s+it\\s+high[-\\s]?level|non\\s+tech)\\b/i;\n+const RE_TECHNICAL = /\\b(deep\\s+technical|go\\s+deep|in\\s+depth|deep\\s+dive)\\b/i;\n+const RE_TIMEFRAME_TODAY = /\\b(answer|deciding)\\s+(today)\\b/i;\n+const RE_TIMEFRAME_THIS_WEEK = /\\b(answer|deciding)\\s+(this\\s+week)\\b/i;\n+const RE_DIRECTNESS = /\\b(straight\\s+answer|bottom\\s+line|no\\s+fluff)\\b/i;\n+\n+/**\n+ * Extracts style constraints without changing any protocol or output shape.\n+ * When multiple constraints apply, the first matching brevity/technical/timeframe is used.\n+ */\n+export function extractStyleConstraints(text: string): StyleConstraints {\n+  const s: StyleConstraints = {};\n+  if (!text) return s;\n+\n+  if (RE_BREVITY_ONE_SENTENCE.test(text)) {\n+    s.brevity = \'one_sentence\';\n+  } else if (RE_BREVITY_SHORT.test(text)) {\n+    s.brevity = \'short\';\n+  }\n+\n+  if (RE_NON_TECHNICAL.test(text)) {\n+    s.technicalLevel = \'non_technical\';\n+  } else if (RE_TECHNICAL.test(text)) {\n+    s.technicalLevel = \'technical\';\n+  }\n+\n+  if (RE_TIMEFRAME_TODAY.test(text)) {\n+    s.timeframe = \'today\';\n+  } else if (RE_TIMEFRAME_THIS_WEEK.test(text)) {\n+    s.timeframe = \'this_week\';\n+  }\n+\n+  if (RE_DIRECTNESS.test(text)) {\n+    s.directness = true;\n+  }\n+\n+  return s;\n+}\n+\n+/**\n+ * Quick boolean to check if any style hint is present.\n+ */\n+export function hasStyleConstraints(text: string): boolean {\n+  const s = extractStyleConstraints(text);\n+  return !!(s.brevity || s.technicalLevel || s.timeframe || s.directness);\n+}\n+\n*** End Patch\n*** Begin Patch\n*** Add File: packages/shared/src/heuristics/patterns/offTopicBridge.ts\n+/**\n+ * Heuristics: Off-topic bridge fallback builder\n+ *\n+ * Goal:\n+ * - Provide a consistent fallback when content is off-topic or confidence is low:\n+ *   clarify → reframe to evaluation criteria → propose next step.\n+ * - Text-only helpers: no side effects, no logging of raw transcript.\n+ */\n+\n+export interface BridgeOptions {\n+  language?: \'en\' | \'es\' | \'fr\';\n+  lowConfidence?: boolean;\n+  // Allows callers to pass in product-agnostic evaluation anchors to keep coaching generic.\n+  anchors?: string[]; // e.g., ["integration", "security", "timeline"]\n+}\n+\n+function defaultAnchors(language: BridgeOptions[\'language\']): string[] {\n+  switch (language) {\n+    case \'es\':\n+      return [\'integración\', \'seguridad\', \'cronograma\'];\n+    case \'fr\':\n+      return [\'intégration\', \'sécurité\', \'calendrier\'];\n+    default:\n+      return [\'integration\', \'security\', \'timeline\'];\n+  }\n+}\n+\n+/**\n+ * Compose a three-step bridge as plain strings.\n+ * The caller decides where/how to display it.\n+ */\n+export function composeOffTopicBridge(opts: BridgeOptions = {}): string[] {\n+  const { language = \'en\', anchors = defaultAnchors(language) } = opts;\n+\n+  if (language === \'es\') {\n+    return [\n+      \'Aclaración rápida: ¿cuál es el criterio principal para que esto sea útil?\',\n+      `Volvamos a la evaluación: ${anchors.join(\', \')}.`,\n+      \'Si ayuda, podemos revisar eso ahora o coordinar el siguiente paso con la persona adecuada.\',\n+    ];\n+  }\n+  if (language === \'fr\') {\n+    return [\n+      \'Petite précision : quel est le critère principal pour être utile ici ?\',\n+      `Recentrons sur l’évaluation : ${anchors.join(\', \')}.`,\n+      \'Si utile, on peut parcourir cela maintenant ou planifier le prochain pas avec le bon interlocuteur.\',\n+    ];\n+  }\n+  // default: \'en\'\n+  return [\n+    "Quick check: what\'s the top criterion so this is helpful?",\n+    `Let\'s tie this back to the evaluation: ${anchors.join(\', \')}.`,\n+    \'If helpful, we can walk through that now or line up the next step with the right stakeholder.\',\n+  ];\n+}\n+\n*** End Patch\n*** Begin Patch\n*** Add File: packages/shared/src/heuristics/patterns/coldCallRouting.ts\n+/**\n+ * Heuristics: Cold-call decision-maker routing\n+ *\n+ * Goal:\n+ * - Detect cues that the current contact isn\'t the decision-maker and prompt routing language.\n+ * - Identify references to decision-maker roles (CFO/finance/procurement/legal/security/IT).\n+ *\n+ * No protocol changes; pure helpers returning booleans/labels.\n+ */\n+\n+const RE_REFERRAL_CUES = [\n+  /\\b(not\\s+the\\s+right\\s+(person|contact))\\b/i,\n+  /\\b(i\'?m\\s+not\\s+(involved|the\\s+one))\\b/i,\n+  /\\b(you\\s+should|please)\\s+(talk|speak)\\s+to\\b/i,\n+  /\\b(X|they)\\s+(handle|own)s?\\s+(that|this)\\b/i,\n+  /\\btry\\s+(it|them|support|procurement)\\b/i,\n+];\n+\n+const RE_DM_ROLES = [\n+  /\\bcfo\\b/i,\n+  /\\b(finance|financial)\\b/i,\n+  /\\b(procurement|purchasing)\\b/i,\n+  /\\blegal\\b/i,\n+  /\\b(security|infosec|secops|it|it\\s+ops)\\b/i,\n+  /\\b(data\\s+protection|privacy|dpo)\\b/i,\n+];\n+\n+const RE_DM_QUESTIONS = [\n+  /\\b(are\\s+you|you\'?re)\\s+(the\\s+)?(decision[-\\s]?maker|budget\\s+owner|sign[-\\s]?off)\\b/i,\n+  /\\bwho\\s+(signs?\\s*off|approves?)\\b/i,\n+  /\\bwho\\s+owns\\s+(budget|this)\\b/i,\n+];\n+\n+export function mentionsDecisionMakerRoles(text: string): boolean {\n+  if (!text) return false;\n+  return RE_DM_ROLES.some((re) => re.test(text));\n+}\n+\n+export function isReferralCue(text: string): boolean {\n+  if (!text) return false;\n+  return RE_REFERRAL_CUES.some((re) => re.test(text));\n+}\n+\n+export function isDecisionMakerQuestion(text: string): boolean {\n+  if (!text) return false;\n+  return RE_DM_QUESTIONS.some((re) => re.test(text));\n+}\n+\n+/**\n+ * Simple router label. Purely advisory:\n+ * - \'route_to_decision_maker\' if there are referral cues or DM roles mentioned.\n+ * - \'probe_decision_maker\' if agent is asking/should ask DM confirmation.\n+ * - \'unknown\' otherwise.\n+ */\n+export type ColdCallRouteLabel = \'route_to_decision_maker\' | \'probe_decision_maker\' | \'unknown\';\n+\n+export function detectColdCallRouting(text: string): ColdCallRouteLabel {\n+  if (!text) return \'unknown\';\n+  if (isReferralCue(text) || mentionsDecisionMakerRoles(text)) return \'route_to_decision_maker\';\n+  if (isDecisionMakerQuestion(text)) return \'probe_decision_maker\';\n+  return \'unknown\';\n+}\n+\n*** End Patch\n*** Begin Patch\n*** Add File: packages/shared/src/heuristics/patterns/index.ts\n+export * from \'./stakeholderSignOff\';\n+export * from \'./styleConstraints\';\n+export * from \'./offTopicBridge\';\n+export * from \'./coldCallRouting\';\n+\n*** End Patch\n*** Begin Patch\n*** Add File: packages/shared/test/heuristics_patterns.test.ts\n+import { detectStakeholderSignOff, scoreStakeholderSignOff } from \'../src/heuristics/patterns/stakeholderSignOff\';\n+import { extractStyleConstraints, hasStyleConstraints } from \'../src/heuristics/patterns/styleConstraints\';\n+import { composeOffTopicBridge } from \'../src/heuristics/patterns/offTopicBridge\';\n+import { detectColdCallRouting } from \'../src/heuristics/patterns/coldCallRouting\';\n+\n+describe(\'heuristics/patterns\', () => {\n+  test(\'detects stakeholder sign-off signals\', () => {\n+    const t1 = \'Our CFO signs off after procurement and legal finish the MSA and DPA security review.\';\n+    expect(detectStakeholderSignOff(t1)).toBe(true);\n+    expect(scoreStakeholderSignOff(t1)).toBeGreaterThanOrEqual(0.5);\n+  });\n+\n+  test(\'extracts style constraints (brevity + non-technical + timeframe)\', () => {\n+    const t = "TL;DR please, keep it short and non-technical — we\'re deciding this week.";\n+    const s = extractStyleConstraints(t);\n+    expect(hasStyleConstraints(t)).toBe(true);\n+    expect(s.brevity).toBe(\'short\');\n+    expect(s.technicalLevel).toBe(\'non_technical\');\n+    expect(s.timeframe).toBe(\'this_week\');\n+  });\n+\n+  test(\'off-topic bridge returns 3 steps in EN/ES/FR\', () => {\n+    expect(composeOffTopicBridge({ language: \'en\' })).toHaveLength(3);\n+    expect(composeOffTopicBridge({ language: \'es\' })).toHaveLength(3);\n+    expect(composeOffTopicBridge({ language: \'fr\' })).toHaveLength(3);\n+  });\n+\n+  test(\'cold-call routing detects referral cues and roles\', () => {\n+    const t1 = \'I am not the right person — you should speak to procurement, they handle that.\';\n+    expect(detectColdCallRouting(t1)).toBe(\'route_to_decision_maker\');\n+    const t2 = \'Are you the budget owner or who signs off?\';\n+    expect(detectColdCallRouting(t2)).toBe(\'probe_decision_maker\');\n+    const t3 = \'Thanks for the call.\';\n+    expect(detectColdCallRouting(t3)).toBe(\'unknown\');\n+  });\n+});\n+\n*** End Patch\nPATCH_END\nFILES_START\n{\n  "packages/shared/src/heuristics/patterns/stakeholderSignOff.ts": "/**\\n * Heuristics: Stakeholder sign-off / procurement detection\\n *\\n * Goal:\\n * - Reduce moment=\\"unknown\\" by catching procurement/legal/security/CFO approval language.\\n * - Keep logic purely regex-based to avoid changing protocol types.\\n *\\n * Notes:\\n * - These exports are additive and safe. They can be optionally wired into existing detectors.\\n * - Do not log raw transcript content here.\\n */\\n\\nexport const STAKEHOLDER_SIGN_OFF_PATTERNS: RegExp[] = [\\n  // Direct approval/sign-off language\\n  /\\\\b(signs?\\\\s*[- ]?\\\\s*off|sign[- ]?off)\\\\b/i,\\n  /\\\\bapprov(e|al|ed|ing)\\\\b/i,\\n  /\\\\bgreen[- ]?light\\\\b/i,\\n\\n  // Budget / finance roles\\n  /\\\\bcfo\\\\b/i,\\n  /\\\\b(finance|financial)\\\\b/i,\\n  /\\\\bbudget(\\\\s+(owner|holder))?\\\\b/i,\\n\\n  // Procurement / purchasing\\n  /\\\\bprocure(ment)?\\\\b/i,\\n  /\\\\bpurchase\\\\s+order\\\\b/i,\\n  /\\\\b(?<!\\\\w)po(?!\\\\w)\\\\b/i, // standalone \\"PO\\"\\n\\n  // Legal and security review\\n  /\\\\blegal\\\\b/i,\\n  /\\\\b(msa|dpa)\\\\b/i,\\n  /\\\\bsecurity\\\\s+review\\\\b/i,\\n  /\\\\b(info\\\\s*sec|infosec|secops)\\\\b/i,\\n];\\n\\n/**\\n * Returns true if the text likely references stakeholder approval,\\n * procurement, legal, or security review checkpoints.\\n */\\nexport function detectStakeholderSignOff(text: string): boolean {\\n  if (!text) return false;\\n  return STAKEHOLDER_SIGN_OFF_PATTERNS.some((re) => re.test(text));\\n}\\n\\n/**\\n * Lightweight classifier for stakeholder-related moment.\\n * Returns a score (0..1) that can be blended with existing models.\\n * - Only uses regex presence count; capped and normalized.\\n */\\nexport function scoreStakeholderSignOff(text: string): number {\\n  if (!text) return 0;\\n  const matches = STAKEHOLDER_SIGN_OFF_PATTERNS.reduce((acc, re) => {\\n    return acc + (re.test(text) ? 1 : 0);\\n  }, 0);\\n  // Cap at 4 signals to keep confidence conservative\\n  const capped = Math.min(matches, 4);\\n  return capped / 4;\\n}\\n",\n  "packages/shared/src/heuristics/patterns/styleConstraints.ts": "/**\\n * Heuristics: Style constraint detection\\n *\\n * Goal:\\n * - Avoid classifying style modifiers as unknown moment.\\n * - Extract guidance like \\"short version\\", \\"non-technical\\", \\"answer today\\".\\n *\\n * No side effects; safe to import from existing detectors/coaches.\\n */\\n\\nexport type BrevityLevel = \'short\' | \'one_sentence\';\\nexport type TechnicalLevel = \'non_technical\' | \'technical\';\\nexport type TimeframeHint = \'today\' | \'this_week\';\\n\\nexport interface StyleConstraints {\\n  brevity?: BrevityLevel;\\n  technicalLevel?: TechnicalLevel;\\n  timeframe?: TimeframeHint;\\n  directness?: boolean; // e.g., \\"straight answer\\"\\n}\\n\\nconst RE_BREVITY_SHORT =\\n  /\\\\b(tl;?dr|short\\\\s+version|keep\\\\s+it\\\\s+short|quick\\\\s+(one|question|answer)|one[-\\\\s]?liner|one\\\\s+liner)\\\\b/i;\\nconst RE_BREVITY_ONE_SENTENCE = /\\\\b(one\\\\s+sentence|in\\\\s+one\\\\s+sentence)\\\\b/i;\\nconst RE_NON_TECHNICAL =\\n  /\\\\b(non[-\\\\s]?technical|explain\\\\s+like\\\\s+i\'?m\\\\s+five|keep\\\\s+it\\\\s+high[-\\\\s]?level|non\\\\s+tech)\\\\b/i;\\nconst RE_TECHNICAL = /\\\\b(deep\\\\s+technical|go\\\\s+deep|in\\\\s+depth|deep\\\\s+dive)\\\\b/i;\\nconst RE_TIMEFRAME_TODAY = /\\\\b(answer|deciding)\\\\s+(today)\\\\b/i;\\nconst RE_TIMEFRAME_THIS_WEEK = /\\\\b(answer|deciding)\\\\s+(this\\\\s+week)\\\\b/i;\\nconst RE_DIRECTNESS = /\\\\b(straight\\\\s+answer|bottom\\\\s+line|no\\\\s+fluff)\\\\b/i;\\n\\n/**\\n * Extracts style constraints without changing any protocol or output shape.\\n * When multiple constraints apply, the first matching brevity/technical/timeframe is used.\\n */\\nexport function extractStyleConstraints(text: string): StyleConstraints {\\n  const s: StyleConstraints = {};\\n  if (!text) return s;\\n\\n  if (RE_BREVITY_ONE_SENTENCE.test(text)) {\\n    s.brevity = \'one_sentence\';\\n  } else if (RE_BREVITY_SHORT.test(text)) {\\n    s.brevity = \'short\';\\n  }\\n\\n  if (RE_NON_TECHNICAL.test(text)) {\\n    s.technicalLevel = \'non_technical\';\\n  } else if (RE_TECHNICAL.test(text)) {\\n    s.technicalLevel = \'technical\';\\n  }\\n\\n  if (RE_TIMEFRAME_TODAY.test(text)) {\\n    s.timeframe = \'today\';\\n  } else if (RE_TIMEFRAME_THIS_WEEK.test(text)) {\\n    s.timeframe = \'this_week\';\\n  }\\n\\n  if (RE_DIRECTNESS.test(text)) {\\n    s.directness = true;\\n  }\\n\\n  return s;\\n}\\n\\n/**\\n * Quick boolean to check if any style hint is present.\\n */\\nexport function hasStyleConstraints(text: string): boolean {\\n  const s = extractStyleConstraints(text);\\n  return !!(s.brevity || s.technicalLevel || s.timeframe || s.directness);\\n}\\n",\n  "packages/shared/src/heuristics/patterns/offTopicBridge.ts": "/**\\n * Heuristics: Off-topic bridge fallback builder\\n *\\n * Goal:\\n * - Provide a consistent fallback when content is off-topic or confidence is low:\\n *   clarify → reframe to evaluation criteria → propose next step.\\n * - Text-only helpers: no side effects, no logging of raw transcript.\\n */\\n\\nexport interface BridgeOptions {\\n  language?: \'en\' | \'es\' | \'fr\';\\n  lowConfidence?: boolean;\\n  // Allows callers to pass in product-agnostic evaluation anchors to keep coaching generic.\\n  anchors?: string[]; // e.g., [\\"integration\\", \\"security\\", \\"timeline\\"]\\n}\\n\\nfunction defaultAnchors(language: BridgeOptions[\'language\']): string[] {\\n  switch (language) {\\n    case \'es\':\\n      return [\'integración\', \'seguridad\', \'cronograma\'];\\n    case \'fr\':\\n      return [\'intégration\', \'sécurité\', \'calendrier\'];\\n    default:\\n      return [\'integration\', \'security\', \'timeline\'];\\n  }\\n}\\n\\n/**\\n * Compose a three-step bridge as plain strings.\\n * The caller decides where/how to display it.\\n */\\nexport function composeOffTopicBridge(opts: BridgeOptions = {}): string[] {\\n  const { language = \'en\', anchors = defaultAnchors(language) } = opts;\\n\\n  if (language === \'es\') {\\n    return [\\n      \'Aclaración rápida: ¿cuál es el criterio principal para que esto sea útil?\',\\n      `Volvamos a la evaluación: ${anchors.join(\', \')}.`,\\n      \'Si ayuda, podemos revisar eso ahora o coordinar el siguiente paso con la persona adecuada.\',\\n    ];\\n  }\\n  if (language === \'fr\') {\\n    return [\\n      \'Petite précision : quel est le critère principal pour être utile ici ?\',\\n      `Recentrons sur l’évaluation : ${anchors.join(\', \')}.`,\\n      \'Si utile, on peut parcourir cela maintenant ou planifier le prochain pas avec le bon interlocuteur.\',\\n    ];\\n  }\\n  // default: \'en\'\\n  return [\\n    \\"Quick check: what\'s the top criterion so this is helpful?\\",\\n    `Let\'s tie this back to the evaluation: ${anchors.join(\', \')}.`,\\n    \'If helpful, we can walk through that now or line up the next step with the right stakeholder.\',\\n  ];\\n}\\n",\n  "packages/shared/src/heuristics/patterns/coldCallRouting.ts": "/**\\n * Heuristics: Cold-call decision-maker routing\\n *\\n * Goal:\\n * - Detect cues that the current contact isn\'t the decision-maker and prompt routing language.\\n * - Identify references to decision-maker roles (CFO/finance/procurement/legal/security/IT).\\n *\\n * No protocol changes; pure helpers returning booleans/labels.\\n */\\n\\nconst RE_REFERRAL_CUES = [\\n  /\\\\b(not\\\\s+the\\\\s+right\\\\s+(person|contact))\\\\b/i,\\n  /\\\\b(i\'?m\\\\s+not\\\\s+(involved|the\\\\s+one))\\\\b/i,\\n  /\\\\b(you\\\\s+should|please)\\\\s+(talk|speak)\\\\s+to\\\\b/i,\\n  /\\\\b(X|they)\\\\s+(handle|own)s?\\\\s+(that|this)\\\\b/i,\\n  /\\\\btry\\\\s+(it|them|support|procurement)\\\\b/i,\\n];\\n\\nconst RE_DM_ROLES = [\\n  /\\\\bcfo\\\\b/i,\\n  /\\\\b(finance|financial)\\\\b/i,\\n  /\\\\b(procurement|purchasing)\\\\b/i,\\n  /\\\\blegal\\\\b/i,\\n  /\\\\b(security|infosec|secops|it|it\\\\s+ops)\\\\b/i,\\n  /\\\\b(data\\\\s+protection|privacy|dpo)\\\\b/i,\\n];\\n\\nconst RE_DM_QUESTIONS = [\\n  /\\\\b(are\\\\s+you|you\'?re)\\\\s+(the\\\\s+)?(decision[-\\\\s]?maker|budget\\\\s+owner|sign[-\\\\s]?off)\\\\b/i,\\n  /\\\\bwho\\\\s+(signs?\\\\s*off|approves?)\\\\b/i,\\n  /\\\\bwho\\\\s+owns\\\\s+(budget|this)\\\\b/i,\\n];\\n\\nexport function mentionsDecisionMakerRoles(text: string): boolean {\\n  if (!text) return false;\\n  return RE_DM_ROLES.some((re) => re.test(text));\\n}\\n\\nexport function isReferralCue(text: string): boolean {\\n  if (!text) return false;\\n  return RE_REFERRAL_CUES.some((re) => re.test(text));\\n}\\n\\nexport function isDecisionMakerQuestion(text: string): boolean {\\n  if (!text) return false;\\n  return RE_DM_QUESTIONS.some((re) => re.test(text));\\n}\\n\\n/**\\n * Simple router label. Purely advisory:\\n * - \'route_to_decision_maker\' if there are referral cues or DM roles mentioned.\\n * - \'probe_decision_maker\' if agent is asking/should ask DM confirmation.\\n * - \'unknown\' otherwise.\\n */\\nexport type ColdCallRouteLabel = \'route_to_decision_maker\' | \'probe_decision_maker\' | \'unknown\';\\n\\nexport function detectColdCallRouting(text: string): ColdCallRouteLabel {\\n  if (!text) return \'unknown\';\\n  if (isReferralCue(text) || mentionsDecisionMakerRoles(text)) return \'route_to_decision_maker\';\\n  if (isDecisionMakerQuestion(text)) return \'probe_decision_maker\';\\n  return \'unknown\';\\n}\\n",\n  "packages/shared/src/heuristics/patterns/index.ts": "export * from \'./stakeholderSignOff\';\\nexport * from \'./styleConstraints\';\\nexport * from \'./offTopicBridge\';\\nexport * from \'./coldCallRouting\';\\n",\n  "packages/shared/test/heuristics_patterns.test.ts": "import { detectStakeholderSignOff, scoreStakeholderSignOff } from \'../src/heuristics/patterns/stakeholderSignOff\';\\nimport { extractStyleConstraints, hasStyleConstraints } from \'../src/heuristics/patterns/styleConstraints\';\\nimport { composeOffTopicBridge } from \'../src/heuristics/patterns/offTopicBridge\';\\nimport { detectColdCallRouting } from \'../src/heuristics/patterns/coldCallRouting\';\\n\\ndescribe(\'heuristics/patterns\', () => {\\n  test(\'detects stakeholder sign-off signals\', () => {\\n    const t1 = \'Our CFO signs off after procurement and legal finish the MSA and DPA security review.\';\\n    expect(detectStakeholderSignOff(t1)).toBe(true);\\n    expect(scoreStakeholderSignOff(t1)).toBeGreaterThanOrEqual(0.5);\\n  });\\n\\n  test(\'extracts style constraints (brevity + non-technical + timeframe)\', () => {\\n    const t = \\"TL;DR please, keep it short and non-technical — we\'re deciding this week.\\";\\n    const s = extractStyleConstraints(t);\\n    expect(hasStyleConstraints(t)).toBe(true);\\n    expect(s.brevity).toBe(\'short\');\\n    expect(s.technicalLevel).toBe(\'non_technical\');\\n    expect(s.timeframe).toBe(\'this_week\');\\n  });\\n\\n  test(\'off-topic bridge returns 3 steps in EN/ES/FR\', () => {\\n    expect(composeOffTopicBridge({ language: \'en\' })).toHaveLength(3);\\n    expect(composeOffTopicBridge({ language: \'es\' })).toHaveLength(3);\\n    expect(composeOffTopicBridge({ language: \'fr\' })).toHaveLength(3);\\n  });\\n\\n  test(\'cold-call routing detects referral cues and roles\', () => {\\n    const t1 = \'I am not the right person — you should speak to procurement, they handle that.\';\\n    expect(detectColdCallRouting(t1)).toBe(\'route_to_decision_maker\');\\n    const t2 = \'Are you the budget owner or who signs off?\';\\n    expect(detectColdCallRouting(t2)).toBe(\'probe_decision_maker\');\\n    const t3 = \'Thanks for the call.\';\\n    expect(detectColdCallRouting(t3)).toBe(\'unknown\');\\n  });\\n});\\n"\n}\nFILES_END'}], 'role': 'assistant'}], 'parallel_tool_calls': True, 'presence_penalty': 0.0, 'previous_response_id': None, 'prompt_cache_key': None, 'prompt_cache_retention': None, 'reasoning': {'effort': 'medium', 'summary': None}, 'safety_identifier': None, 'service_tier': 'default', 'store': True, 'temperature': 1.0, 'text': {'format': {'type': 'text'}, 'verbosity': 'medium'}, 'tool_choice': 'auto', 'tools': [], 'top_logprobs': 0, 'top_p': 1.0, 'truncation': 'disabled', 'usage': {'input_tokens': 5184, 'input_tokens_details': {'cached_tokens': 0}, 'output_tokens': 9040, 'output_tokens_details': {'reasoning_tokens': 2368}, 'total_tokens': 14224}, 'user': None, 'metadata': {}}