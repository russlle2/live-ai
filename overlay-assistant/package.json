{
  "name": "overlay-assistant",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -C packages/shared build && concurrently -n shared,server,web -c auto \"pnpm -C packages/shared dev\" \"pnpm -C apps/server dev\" \"pnpm -C apps/web dev\"",
    "dev:server": "pnpm -C apps/server dev",
    "dev:web": "pnpm -C apps/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -C packages/shared build && pnpm -r lint",
    "test": "pnpm -C packages/shared build && pnpm -r test",
    "typecheck": "pnpm -C packages/shared build && pnpm -r typecheck",
    "check": "pnpm -C packages/shared build && pnpm -r lint && pnpm -r typecheck && pnpm -r test",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "prettier": "^3.3.3"
  }
}
